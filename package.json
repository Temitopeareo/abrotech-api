{
  "name": "upload-to-catbox",
  "version": "1.0.0",
  "main": "api/upload-to-catbox.js",
  "scripts": {
    "start": "node api/upload-to-catbox.js"
  },
  "dependencies": {
    "form-data": "^4.0.0",
    "node-fetch": "^3.3.2",
    "stream": "0.0.2"  // Ensure stream package is included if needed
  },
  "engines": {
    "node": "18.x"  // Match the Node.js version used in Vercel
  }
}
